# if [[ $1 == --nmtui ]]; then
# nmtui
# exit 0
# fi

# bssid=$(nmcli device wifi list | sed -n '1!p' | cut -b 28- | dmenu -i -p  "📶 " -l 10 | cut -d' ' -f1); pass=$(printf "" | dmenu -P -p "🔑 ")
# sudo nmcli device wifi connect $bssid password $pass && bssid_name=$(nmcli device wifi show | grep SSID | cut -d' ' -f2) && notify-send "WIFI Connect Sucessfully to $bssid_name"

# # wifi_setup --nmtui  ## can lanuch a nmtui window with st 
# # wifi_setup - wifi off/on feature > nmcli radio wifi off/on
# # and no password enter at every time
# # fast run a inital , not scan every time
st -e nmtui-connect