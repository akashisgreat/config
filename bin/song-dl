#!/bin/bash

if [[ $1 == '-f' ]]; then
  filename="$2"
  while read line; do
    echo "Downlaoding: $line"
    yt-dlp --embed-thumbnail -x --audio-format mp3 "ytsearch:$line" 
  done < "$filename"
  exit
fi

 yt-dlp --embed-thumbnail -x --audio-format mp3 "$1"
