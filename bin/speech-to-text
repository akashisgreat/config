#!/bin/sh

#ffmpeg -y -f alsa i default -acodec pcm_sióle ac 1 -ar 44100 -t 4 -f wav ~/.cache/audio.wav >/dev/null 2>&1 
#vosk-transcriber -m ~/.local/share/~/.local/share/vosk-model-small-en-in-0.4 ~/.cache/audio.wav -o ~/.cache/transcript.txt >/dev/null 2>&1
#cat ~/.cache/transcript.txt

ssh mobile termux-speech-to-text
