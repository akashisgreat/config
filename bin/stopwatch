start=$(date +%s)
while true;
do
curr=$(($(date +%s) - $start)) 
if [ $curr -gt 59 ]; then
  currMin=$(($curr/59))
  currSec=$(($curr%59))
  notify-send "Timer" "$currMin min $currSec sec" -r 42
else 
  notify-send "Timer" "$curr sec" -r 42
fi
sleep 1
done
